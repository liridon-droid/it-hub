node_modules/
dist/
.vite/
.DS_Store
.env
.env.local

# Claude Code local session state
.claude/

# User uploads — runtime data, not source
server/uploads/

# Logs
*.log
npm-debug.log*
.module-state.json

# Local dev process logs
.dev-logs/

# Build output of the guide-editor island (vite.editor.config.js). Generated by
# `npm run build:editor`, which `npm run build` and bin/dev.sh both run — so it
# is never checked in even though it lives under public/.
public/assets/guide-editor.js
public/assets/guide-editor.js.map
public/assets/guide-editor.css
